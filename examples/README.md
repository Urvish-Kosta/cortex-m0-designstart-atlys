# Examples

- `serial-session.txt` — an illustrative walkthrough of the UART console
  interaction, matching the strings in `firmware/src/main.c`. It is written
  from the firmware source, not captured from hardware; replace with a real
  terminal log when available.
